RSpec.describe FastlanePluginExpo do
  it "has a version number" do
    expect(FastlanePluginExpo::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
